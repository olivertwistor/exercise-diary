<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="main_menu_button">
        <item name="android:textSize">@dimen/large_text_size</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:padding">@dimen/standard_padding</item>
    </style>

    <style name="form_label">
        <item name="android:textSize">@dimen/large_text_size</item>
        <item name="android:textColor">@color/form_text</item>
        <item name="android:padding">@dimen/standard_padding</item>
    </style>

    <style name="form_input" parent="form_label"/>

    <style name="form_button" parent="main_menu_button">
        <item name="android:layout_margin">24dp</item>
    </style>

</resources>
